{
  "name": "twitch-subs-only",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "mv subscribers dist",
    "serve": "vite preview"
  },
  "dependencies": {
    "cookie": "^0.4.1",
    "node-fetch": "^2.6.1",
    "preact": "^10.5.13"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.0.0",
    "vite": "^2.5.1"
  }
}
